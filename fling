--// SERVICES
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

local player = Players.LocalPlayer
local LocalPlayer = player
local Camera = Workspace.CurrentCamera

--// FLING VARIABLES & SETTINGS
local SelectedTargets = {}
local PlayerCheckboxes = {}
local FlingActive = false
getgenv().OldPos = nil
getgenv().FPDH = Workspace.FallenPartsDestroyHeight

--// UI THEMES
local CurrentTheme = "Dark"
local Themes = {
    Dark = {
        Background = Color3.fromRGB(10, 10, 10),
        TitleBar = Color3.fromRGB(15, 15, 15),
        Button = Color3.fromRGB(25, 25, 25),
        ButtonHover = Color3.fromRGB(35, 35, 35),
        Text = Color3.fromRGB(220, 220, 220),
        Border = Color3.fromRGB(60, 60, 60),
        TopButtons = Color3.fromRGB(40, 40, 40),
        AuthorBar = Color3.fromRGB(15, 15, 15)
    },
    Light = {
        Background = Color3.fromRGB(240, 240, 240),
        TitleBar = Color3.fromRGB(220, 220, 220),
        Button = Color3.fromRGB(255, 255, 255),
        ButtonHover = Color3.fromRGB(230, 230, 230),
        Text = Color3.fromRGB(20, 20, 20),
        Border = Color3.fromRGB(180, 180, 180),
        TopButtons = Color3.fromRGB(200, 200, 200),
        AuthorBar = Color3.fromRGB(220, 220, 220)
    }
}

-- 縮小整體選單尺寸
local UIWidth = 210
local UIHeight = 300

--// MAIN SCREEN GUI
local MainGui = Instance.new("ScreenGui", CoreGui)
MainGui.Name = "XcxHubMultiFling"
MainGui.ResetOnSpawn = false

-- 主面板
local Frame = Instance.new("Frame", MainGui)
Frame.Size = UDim2.new(0, UIWidth, 0, UIHeight) 
Frame.Position = UDim2.new(0.5, -(UIWidth/2), 0.5, -(UIHeight/2))
Frame.BackgroundColor3 = Themes.Dark.Background
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.ClipsDescendants = true 

local FrameCorner = Instance.new("UICorner", Frame)
FrameCorner.CornerRadius = UDim.new(0, 10)

local FrameStroke = Instance.new("UIStroke", Frame)
FrameStroke.Thickness = 1.5
FrameStroke.Color = Themes.Dark.Border
FrameStroke.Transparency = 0.2

-- 標題欄 (高度縮減)
local TitleBar = Instance.new("Frame", Frame)
TitleBar.Size = UDim2.new(1, 0, 0, 30)
TitleBar.BackgroundColor3 = Themes.Dark.TitleBar
TitleBar.BorderSizePixel = 0

local TitleCorner = Instance.new("UICorner", TitleBar)
TitleCorner.CornerRadius = UDim.new(0, 10)

local TitleFix = Instance.new("Frame", TitleBar)
TitleFix.Size = UDim2.new(1, 0, 0, 10)
TitleFix.Position = UDim2.new(0, 0, 1, -10)
TitleFix.BackgroundColor3 = Themes.Dark.TitleBar
TitleFix.BorderSizePixel = 0

local Title = Instance.new("TextLabel", TitleBar)
Title.Size = UDim2.new(1, -90, 1, 0)
Title.Position = UDim2.new(0, 10, 0, 0)
Title.Text = "Xcx Hub || Multi Fling"
Title.TextColor3 = Themes.Dark.Text
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.TextSize = 11
Title.TextXAlignment = Enum.TextXAlignment.Left

-- 狀態提示列
local StatusLabel = Instance.new("TextLabel", Frame)
StatusLabel.Size = UDim2.new(1, -20, 0, 18)
StatusLabel.Position = UDim2.new(0, 10, 0, 34)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "0 target(s) selected"
StatusLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
StatusLabel.Font = Enum.Font.GothamSemibold
StatusLabel.TextSize = 10
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left

-- 拖曳機制
local dragging, dragInput, dragStart, startPos
TitleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true; dragStart = input.Position; startPos = Frame.Position
        input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
    end
end)
TitleBar.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end end)
UserInputService.InputChanged:Connect(function(input) if input == dragInput and dragging then local delta = input.Position - dragStart; Frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end end)

-- 頂部功能按鈕 (縮小尺寸)
local SettingsBtn = Instance.new("TextButton", TitleBar)
SettingsBtn.Size = UDim2.new(0, 22, 0, 22)
SettingsBtn.Position = UDim2.new(1, -76, 0.5, -11)
SettingsBtn.BackgroundColor3 = Themes.Dark.TopButtons
SettingsBtn.Text = "⚙"
SettingsBtn.TextColor3 = Themes.Dark.Text
SettingsBtn.Font = Enum.Font.GothamBold
SettingsBtn.TextSize = 12

local MinimizeBtn = Instance.new("TextButton", TitleBar)
MinimizeBtn.Size = UDim2.new(0, 22, 0, 22)
MinimizeBtn.Position = UDim2.new(1, -50, 0.5, -11)
MinimizeBtn.BackgroundColor3 = Themes.Dark.TopButtons
MinimizeBtn.Text = "−"
MinimizeBtn.TextColor3 = Themes.Dark.Text
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.TextSize = 12

local CloseBtn = Instance.new("TextButton", TitleBar)
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Position = UDim2.new(1, -24, 0.5, -11)
CloseBtn.BackgroundColor3 = Themes.Dark.TopButtons
CloseBtn.Text = "×"
CloseBtn.TextColor3 = Themes.Dark.Text
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 14

Instance.new("UICorner", SettingsBtn).CornerRadius = UDim.new(0, 6)
Instance.new("UICorner", MinimizeBtn).CornerRadius = UDim.new(0, 6)
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 6)

-- 玩家清單滾動框架 (等比例縮小高度與邊距)
local PlayerListScroll = Instance.new("ScrollingFrame", Frame)
PlayerListScroll.Size = UDim2.new(1, -12, 0, 130)
PlayerListScroll.Position = UDim2.new(0, 6, 0, 54)
PlayerListScroll.BackgroundTransparency = 1
PlayerListScroll.BorderSizePixel = 0
PlayerListScroll.ScrollBarThickness = 2
PlayerListScroll.ScrollBarImageColor3 = Color3.fromRGB(100, 100, 100)

-- 底部按鈕區與作者容器
local ControlsFrame = Instance.new("Frame", Frame)
ControlsFrame.Size = UDim2.new(1, -12, 0, 102)
ControlsFrame.Position = UDim2.new(0, 6, 1, -108)
ControlsFrame.BackgroundTransparency = 1

--// HELPER FUNCTIONS
local function CountSelectedTargets()
    local count = 0
    for _ in pairs(SelectedTargets) do count = count + 1 end
    return count
end

local function UpdateStatus()
    local count = CountSelectedTargets()
    if FlingActive then
        StatusLabel.Text = "🔥 Flinging " .. count .. " target(s)..."
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
    else
        StatusLabel.Text = "👤 " .. count .. " target(s) selected"
        StatusLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
    end
end

local function Message(title, text, duration)
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = title,
            Text = text,
            Duration = duration or 3
        })
    end)
end

--// REFRESH PLAYER LIST SYSTEM
local function RefreshPlayerList()
    for _, child in pairs(PlayerListScroll:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    PlayerCheckboxes = {}

    local list = Players:GetPlayers()
    table.sort(list, function(a, b) return a.Name:lower() < b.Name:lower() end)

    local yOffset = 0
    for _, p in ipairs(list) do
        if p == LocalPlayer then continue end

        local row = Instance.new("Frame", PlayerListScroll)
        row.Size = UDim2.new(1, -4, 0, 28)
        row.Position = UDim2.new(0, 2, 0, yOffset)
        row.BackgroundColor3 = Themes[CurrentTheme].Button
        row.BorderSizePixel = 0
        Instance.new("UICorner", row).CornerRadius = UDim.new(0, 5)

        local checkIndicator = Instance.new("Frame", row)
        checkIndicator.Size = UDim2.new(0, 3, 1, 0)
        checkIndicator.Position = UDim2.new(0, 0, 0, 0)
        checkIndicator.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
        checkIndicator.BorderSizePixel = 0
        checkIndicator.Visible = (SelectedTargets[p.Name] ~= nil)
        Instance.new("UICorner", checkIndicator).CornerRadius = UDim.new(0, 5)

        local nameLabel = Instance.new("TextLabel", row)
        nameLabel.Size = UDim2.new(1, -20, 1, 0)
        nameLabel.Position = UDim2.new(0, 10, 0, 0)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Text = p.Name
        nameLabel.TextColor3 = Themes[CurrentTheme].Text
        nameLabel.Font = Enum.Font.GothamSemibold
        nameLabel.TextSize = 10
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left

        local hitBtn = Instance.new("TextButton", row)
        hitBtn.Size = UDim2.new(1, 0, 1, 0)
        hitBtn.BackgroundTransparency = 1
        hitBtn.Text = ""

        hitBtn.MouseButton1Click:Connect(function()
            if SelectedTargets[p.Name] then
                SelectedTargets[p.Name] = nil
                checkIndicator.Visible = false
            else
                SelectedTargets[p.Name] = p
                checkIndicator.Visible = true
            end
            UpdateStatus()
        end)

        PlayerCheckboxes[p.Name] = { Indicator = checkIndicator }
        yOffset = yOffset + 32
    end
    PlayerListScroll.CanvasSize = UDim2.new(0, 0, 0, yOffset)
end

local function ToggleAllPlayers(select)
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            if select then
                SelectedTargets[p.Name] = p
                if PlayerCheckboxes[p.Name] then PlayerCheckboxes[p.Name].Indicator.Visible = true end
            else
                SelectedTargets[p.Name] = nil
                if PlayerCheckboxes[p.Name] then PlayerCheckboxes[p.Name].Indicator.Visible = false end
            end
        end
    end
    UpdateStatus()
end

--// THE FLING ENGINE
local function SkidFling(TargetPlayer)
    local Character = LocalPlayer.Character
    local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
    local RootPart = Humanoid and Humanoid.RootPart
    local TCharacter = TargetPlayer.Character
    if not TCharacter then return end
    
    local THumanoid = TCharacter:FindFirstChildOfClass("Humanoid")
    local TRootPart = THumanoid and THumanoid.RootPart
    local THead = TCharacter:FindFirstChild("Head")
    local Accessory = TCharacter:FindFirstChildOfClass("Accessory")
    local Handle = Accessory and Accessory:FindFirstChild("Handle")

    if Character and Humanoid and RootPart then
        if RootPart.Velocity.Magnitude < 50 then getgenv().OldPos = RootPart.CFrame end
        if THumanoid and THumanoid.Sit then return Message("Notice", TargetPlayer.Name .. " is sitting", 2) end
        
        if THead then Camera.CameraSubject = THead 
        elseif Handle then Camera.CameraSubject = Handle 
        elseif THumanoid and TRootPart then Camera.CameraSubject = THumanoid end
        
        if not TCharacter:FindFirstChildWhichIsA("BasePart") then return end

        local FPos = function(BasePart, Pos, Ang)
            RootPart.CFrame = CFrame.new(BasePart.Position) * Pos * Ang
            Character:SetPrimaryPartCFrame(CFrame.new(BasePart.Position) * Pos * Ang)
            RootPart.Velocity = Vector3.new(9e7, 9e7 * 10, 9e7)
            RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
        end

        local SFBasePart = function(BasePart)
            local TimeToWait = 1.5
            local Time = tick()
            local Angle = 0
            repeat
                if RootPart and THumanoid then
                    if BasePart.Velocity.Magnitude < 50 then
                        Angle = Angle + 100
                        FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle),0 ,0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                        task.wait()
                    else
                        FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, -THumanoid.WalkSpeed), CFrame.Angles(0, 0, 0))
                        task.wait()
                    end
                end
            until Time + TimeToWait < tick() or not FlingActive
        end

        Workspace.FallenPartsDestroyHeight = 0/0
        local BV = Instance.new("BodyVelocity", RootPart)
        BV.Velocity = Vector3.new(0, 0, 0)
        BV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)

        if TRootPart then SFBasePart(TRootPart)
        elseif THead then SFBasePart(THead)
        elseif Handle then SFBasePart(Handle) end

        BV:Destroy()
        Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
        Camera.CameraSubject = Humanoid

        if getgenv().OldPos then
            repeat
                RootPart.CFrame = getgenv().OldPos * CFrame.new(0, .5, 0)
                Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0, .5, 0))
                Humanoid:ChangeState("GettingUp")
                for _, part in pairs(Character:GetChildren()) do
                    if part:IsA("BasePart") then part.Velocity, part.RotVelocity = Vector3.new(), Vector3.new() end
                end
                task.wait()
            until (RootPart.Position - getgenv().OldPos.p).Magnitude < 25
            Workspace.FallenPartsDestroyHeight = getgenv().FPDH
        end
    end
end

--// CONTROLLER LOOPS
local function StartFling()
    if FlingActive then return end
    if CountSelectedTargets() == 0 then
        StatusLabel.Text = "❌ No targets selected!"
        task.wait(1.5) UpdateStatus() return
    end
    FlingActive = true
    UpdateStatus()
    
    task.spawn(function()
        while FlingActive do
            local validTargets = {}
            for name, p in pairs(SelectedTargets) do
                if p and p.Parent then validTargets[name] = p
                else SelectedTargets[name] = nil end
            end
            
            for _, p in pairs(validTargets) do
                if FlingActive then SkidFling(p) task.wait(0.1) else break end
            end
            UpdateStatus()
            task.wait(0.3)
        end
    end)
end

--// UI BUTTON GENERATOR (微調高度與間距)
local buttonReferences = {}
local function CreateControlBtn(text, size, pos, bgCol, callback)
    local btn = Instance.new("TextButton", ControlsFrame)
    btn.Size = size
    btn.Position = pos
    btn.BackgroundColor3 = bgCol or Themes[CurrentTheme].Button
    btn.Text = text
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 10 -- 字型小一點
    btn.BorderSizePixel = 0
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)

    btn.MouseButton1Click:Connect(callback)
    table.insert(buttonReferences, btn)
    return btn
end

-- 生成核心控制按鈕 (縮小各按鈕的尺寸)
local StartBtn = CreateControlBtn("START FLING", UDim2.new(0.5, -3, 0, 32), UDim2.new(0, 0, 0, 0), Color3.fromRGB(0, 160, 90), StartFling)
local StopBtn = CreateControlBtn("STOP FLING", UDim2.new(0.5, -3, 0, 32), UDim2.new(0.5, 3, 0, 0), Color3.fromRGB(200, 60, 60), function() FlingActive = false UpdateStatus() Message("Stopped", "Fling process killed", 2) end)
local AllBtn = CreateControlBtn("SELECT ALL", UDim2.new(0.5, -3, 0, 24), UDim2.new(0, 0, 0, 36), Color3.fromRGB(50, 50, 50), function() ToggleAllPlayers(true) end)
local NoneBtn = CreateControlBtn("DESELECT ALL", UDim2.new(0.5, -3, 0, 24), UDim2.new(0.5, 3, 0, 36), Color3.fromRGB(50, 50, 50), function() ToggleAllPlayers(false) end)

-- 【作者署名列】精準定位在 SELECT / DESELECT ALL 按鈕下方 (同步縮小體積)
local AuthorBar = Instance.new("Frame", ControlsFrame)
AuthorBar.Size = UDim2.new(1, 0, 0, 22)
AuthorBar.Position = UDim2.new(0, 0, 0, 66) -- 36 (按鈕Y) + 24 (按鈕高) + 6 (間距)
AuthorBar.BackgroundColor3 = Themes.Dark.AuthorBar
AuthorBar.BorderSizePixel = 0

local AuthorBarCorner = Instance.new("UICorner", AuthorBar)
AuthorBarCorner.CornerRadius = UDim.new(0, 5)

local AuthorLabel = Instance.new("TextLabel", AuthorBar)
AuthorLabel.Size = UDim2.new(1, 0, 1, 0)
AuthorLabel.BackgroundTransparency = 1
AuthorLabel.Text = "by: romokaso & mod2 pure visual"
AuthorLabel.TextColor3 = Color3.fromRGB(140, 140, 140)
AuthorLabel.Font = Enum.Font.GothamBold
AuthorLabel.TextSize = 9 -- 字型調小
AuthorLabel.TextXAlignment = Enum.TextXAlignment.Center

--// SETTINGS & CONFIRM SUB PANELS
local SettingsFrame = Instance.new("Frame", Frame)
SettingsFrame.Size = UDim2.new(1, 0, 1, 0)
SettingsFrame.BackgroundColor3 = Themes.Dark.Background
SettingsFrame.Visible = false
SettingsFrame.ZIndex = 20
Instance.new("UICorner", SettingsFrame).CornerRadius = UDim.new(0, 10)

local SettingsTitle = Instance.new("TextLabel", SettingsFrame)
SettingsTitle.Size = UDim2.new(1, 0, 0, 35)
SettingsTitle.Position = UDim2.new(0, 0, 0, 5)
SettingsTitle.Text = "SETTINGS"
SettingsTitle.TextColor3 = Themes.Dark.Text
SettingsTitle.Font = Enum.Font.GothamBold
SettingsTitle.TextSize = 12
SettingsTitle.BackgroundTransparency = 1
SettingsTitle.ZIndex = 21

local ThemeLabel = Instance.new("TextLabel", SettingsFrame)
ThemeLabel.Size = UDim2.new(1, -20, 0, 20)
ThemeLabel.Position = UDim2.new(0, 12, 0, 40)
ThemeLabel.Text = "THEME:"
ThemeLabel.TextColor3 = Themes.Dark.Text
ThemeLabel.Font = Enum.Font.GothamBold
ThemeLabel.TextSize = 10
ThemeLabel.BackgroundTransparency = 1
ThemeLabel.TextXAlignment = Enum.TextXAlignment.Left
ThemeLabel.ZIndex = 21

local DarkThemeBtn = Instance.new("TextButton", SettingsFrame)
DarkThemeBtn.Size = UDim2.new(0.45, -5, 0, 28)
DarkThemeBtn.Position = UDim2.new(0, 12, 0, 65)
DarkThemeBtn.BackgroundColor3 = Color3.fromRGB(50, 150, 100)
DarkThemeBtn.Text = "DARK"
DarkThemeBtn.TextColor3 = Color3.new(1,1,1)
DarkThemeBtn.Font = Enum.Font.GothamBold
DarkThemeBtn.TextSize = 10
DarkThemeBtn.ZIndex = 21

local LightThemeBtn = Instance.new("TextButton", SettingsFrame)
LightThemeBtn.Size = UDim2.new(0.45, -5, 0, 28)
LightThemeBtn.Position = UDim2.new(0.55, -2, 0, 65)
LightThemeBtn.BackgroundColor3 = Themes.Dark.Button
LightThemeBtn.Text = "LIGHT"
LightThemeBtn.TextColor3 = Themes.Dark.Text
LightThemeBtn.Font = Enum.Font.GothamBold
LightThemeBtn.TextSize = 10
LightThemeBtn.ZIndex = 21

local BackBtn = Instance.new("TextButton", SettingsFrame)
BackBtn.Size = UDim2.new(0.9, 0, 0, 28)
BackBtn.Position = UDim2.new(0.05, 0, 1, -40)
BackBtn.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
BackBtn.Text = "BACK"
BackBtn.TextColor3 = Color3.new(1,1,1)
BackBtn.Font = Enum.Font.GothamBold
BackBtn.TextSize = 10
BackBtn.ZIndex = 21

Instance.new("UICorner", DarkThemeBtn).CornerRadius = UDim.new(0, 6)
Instance.new("UICorner", LightThemeBtn).CornerRadius = UDim.new(0, 6)
Instance.new("UICorner", BackBtn).CornerRadius = UDim.new(0, 6)

local ConfirmFrame = Instance.new("Frame", Frame)
ConfirmFrame.Size = UDim2.new(1, 0, 1, 0)
ConfirmFrame.BackgroundColor3 = Themes.Dark.Background
ConfirmFrame.Visible = false
ConfirmFrame.ZIndex = 25
ConfirmFrame.BackgroundTransparency = 0.05
Instance.new("UICorner", ConfirmFrame).CornerRadius = UDim.new(0, 10)

local ConfirmText = Instance.new("TextLabel", ConfirmFrame)
ConfirmText.Size = UDim2.new(1, -20, 0, 50)
ConfirmText.Position = UDim2.new(0, 10, 0, 70)
ConfirmText.Text = "Are you sure you want\nto close the GUI?"
ConfirmText.TextColor3 = Themes.Dark.Text
ConfirmText.Font = Enum.Font.GothamBold
ConfirmText.TextSize = 11
ConfirmText.BackgroundTransparency = 1
ConfirmText.ZIndex = 26

local YesBtn = Instance.new("TextButton", ConfirmFrame)
YesBtn.Size = UDim2.new(0, 80, 0, 28)
YesBtn.Position = UDim2.new(0, 16, 1, -50)
YesBtn.BackgroundColor3 = Color3.fromRGB(50, 180, 100)
YesBtn.Text = "YES"
YesBtn.TextColor3 = Color3.new(1,1,1)
YesBtn.Font = Enum.Font.GothamBold
YesBtn.TextSize = 10
YesBtn.ZIndex = 26

local NoBtn = Instance.new("TextButton", ConfirmFrame)
NoBtn.Size = UDim2.new(0, 80, 0, 28)
NoBtn.Position = UDim2.new(1, -96, 1, -50)
NoBtn.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
NoBtn.Text = "NO"
NoBtn.TextColor3 = Color3.new(1,1,1)
NoBtn.Font = Enum.Font.GothamBold
NoBtn.TextSize = 10
NoBtn.ZIndex = 26

Instance.new("UICorner", YesBtn).CornerRadius = UDim.new(0, 6)
Instance.new("UICorner", NoBtn).CornerRadius = UDim.new(0, 6)

--// THEME ENGINE IMPLEMENTATION
local function ApplyTheme(theme)
    local colors = Themes[theme]
    CurrentTheme = theme
    Frame.BackgroundColor3 = colors.Background
    TitleBar.BackgroundColor3 = colors.TitleBar
    TitleFix.BackgroundColor3 = colors.TitleBar
    Title.TextColor3 = colors.Text
    SettingsBtn.BackgroundColor3 = colors.TopButtons
    SettingsBtn.TextColor3 = colors.Text
    MinimizeBtn.BackgroundColor3 = colors.TopButtons
    MinimizeBtn.TextColor3 = colors.Text
    CloseBtn.BackgroundColor3= colors.TopButtons
    CloseBtn.TextColor3 = colors.Text
    AuthorBar.BackgroundColor3 = colors.AuthorBar

    SettingsFrame.BackgroundColor3 = colors.Background
    SettingsTitle.TextColor3 = colors.Text
    ThemeLabel.TextColor3 = colors.Text
    ConfirmFrame.BackgroundColor3 = colors.Background
    ConfirmText.TextColor3 = colors.Text

    if theme == "Dark" then
        DarkThemeBtn.BackgroundColor3 = Color3.fromRGB(50, 150, 100)
        LightThemeBtn.BackgroundColor3 = colors.Button
        LightThemeBtn.TextColor3 = colors.Text
        AuthorLabel.TextColor3 = Color3.fromRGB(140, 140, 140)
    else
        LightThemeBtn.BackgroundColor3 = Color3.fromRGB(50, 150, 100)
        DarkThemeBtn.BackgroundColor3 = colors.Button
        DarkThemeBtn.TextColor3 = colors.Text
        AuthorLabel.TextColor3 = Color3.fromRGB(100, 100, 100)
    end
    RefreshPlayerList()
end

--// WINDOW ANIMATION CONNECTIONS
local isMinimized, isClosing, minimizeCooldown, closeCooldown, confirmDialogOpen, settingsOpen = false, false, false, false, false, false

DarkThemeBtn.MouseButton1Click:Connect(function() ApplyTheme("Dark") end)
LightThemeBtn.MouseButton1Click:Connect(function() ApplyTheme("Light") end)
BackBtn.MouseButton1Click:Connect(function() SettingsFrame.Visible = false; settingsOpen = false end)

SettingsBtn.MouseButton1Click:Connect(function()
    if confirmDialogOpen then return end
    if isMinimized then
        isMinimized = false
        TweenService:Create(Frame, TweenInfo.new(0.1), {Size = UDim2.new(0, UIWidth, 0, UIHeight)}):Play()
        MinimizeBtn.Text = "−"
        task.wait(0.1)
    end
    SettingsFrame.Visible = not SettingsFrame.Visible
    settingsOpen = SettingsFrame.Visible
end)

MinimizeBtn.MouseButton1Click:Connect(function()
    if minimizeCooldown or confirmDialogOpen then return end
    minimizeCooldown = true
    if settingsOpen then SettingsFrame.Visible = false; settingsOpen = false end
    isMinimized = not isMinimized
    if isMinimized then
        TweenService:Create(Frame, TweenInfo.new(0.1), {Size = UDim2.new(0, UIWidth, 0, 30)}):Play()
        MinimizeBtn.Text = "+"
    else
        TweenService:Create(Frame, TweenInfo.new(0.1), {Size = UDim2.new(0, UIWidth, 0, UIHeight)}):Play()
        MinimizeBtn.Text = "−"
    end
    task.wait(0.1)
    minimizeCooldown = false
end)

CloseBtn.MouseButton1Click:Connect(function()
    if closeCooldown or isClosing or confirmDialogOpen then return end
    closeCooldown = true; confirmDialogOpen = true
    if settingsOpen then SettingsFrame.Visible = false; settingsOpen = false end
    if isMinimized then
        isMinimized = false
        TweenService:Create(Frame, TweenInfo.new(0.1), {Size = UDim2.new(0, UIWidth, 0, UIHeight)}):Play()
        MinimizeBtn.Text = "−"
        task.wait(0.1)
    end
    ConfirmFrame.Visible = true
    ConfirmFrame.BackgroundTransparency = 1
    TweenService:Create(ConfirmFrame, TweenInfo.new(0.1), {BackgroundTransparency = 0.05}):Play()
    task.wait(0.1)
    closeCooldown = false
end)

NoBtn.MouseButton1Click:Connect(function()
    TweenService:Create(ConfirmFrame, TweenInfo.new(0.1), {BackgroundTransparency = 1}):Play()
    task.wait(0.1)
    ConfirmFrame.Visible = false; confirmDialogOpen = false
end)

YesBtn.MouseButton1Click:Connect(function()
    isClosing = true
    TweenService:Create(ConfirmFrame, TweenInfo.new(0.1), {BackgroundTransparency = 1}):Play()
    task.wait(0.1)
    TweenService:Create(Frame, TweenInfo.new(0.1, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0), Position = UDim2.new(0.5, 0, 0.5, 0)}):Play()
    task.wait(0.1)
    FlingActive = false
    MainGui:Destroy()
end)

Players.PlayerAdded:Connect(RefreshPlayerList)
Players.PlayerRemoving:Connect(function(p)
    if SelectedTargets[p.Name] then SelectedTargets[p.Name] = nil end
    RefreshPlayerList()
    UpdateStatus()
end)

--// INITIALIZE & OPENING ANIMATION
RefreshPlayerList()
UpdateStatus()

Frame.Size = UDim2.new(0, 0, 0, 0)
Frame.Position = UDim2.new(0.5, 0, 0.5, 0)
TweenService:Create(Frame, TweenInfo.new(0.1, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, UIWidth, 0, UIHeight),
    Position = UDim2.new(0.5, -(UIWidth/2), 0.5, -(UIHeight/2))
}):Play()

Message("Loaded", "Xcx Hub Multi-Fling Active!", 3)
